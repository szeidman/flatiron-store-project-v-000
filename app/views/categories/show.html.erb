<h1><%= @category.title %></h1>

<ul>
<% @category.items.each do |item| %>
    <li><%= link_to item.title, item_path(item) %> <%= "$#{item.price.to_f/100}" %> <%= button_to "Add to Cart", @category if !!current_user %></li>
<% end %>
</ul>
